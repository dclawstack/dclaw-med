# Environment
.env
.env.local
.env.*.local

# OS / IDE
.DS_Store
.idea/
.vscode/
.obsidian/
*.swp
*.swo
*~

# Docker
*.pid

# Kubernetes / Helm
*.tgz
helm/**/charts/

# Design tokens scratchpad kept at repo root, not wired into the app.
/colors_and_type.css

# Knowledge-graph build output (regenerated by `graphify update`).
graphify-out/

# Local dev artifacts
backend/dev.db
backend/.venv/
backend/.pytest_cache/
backend/**/__pycache__/

# TestSprite scratch (rejected during one session; ignore if it ever
# gets re-bootstrapped locally).
.testsprite/
testsprite_tests/

# Playwright e2e — install per-machine, never commit installs/results.
tests/e2e/node_modules/
tests/e2e/playwright-report/
tests/e2e/test-results/

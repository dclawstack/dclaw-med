apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-med.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-med.name" . }}-backend
    {{- include "dclaw-med.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-med.name" . }}-backend
    {{- include "dclaw-med.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-med.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-med.name" . }}-frontend
    {{- include "dclaw-med.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-med.name" . }}-frontend
    {{- include "dclaw-med.selectorLabels" . | nindent 4 }}
